# AI Usage Widget Implementation Status

Updated: 2026-06-17

## Latest Continuation Note

- 2026-06-17 release/signing continuation: the project now uses the personal bundle namespace `com.superzhangkai.*` and App Group `group.com.superzhangkai.aiusagewidget`. Local personal Apple Development, Apple Distribution, Developer ID Application, and Developer ID Installer identities are present. `npm run device:install -- --allow-provisioning-updates` installed `AIUsageWidgetApp` on the connected iPhone and wrote token-safe install evidence under `outputs/device-evidence/`. GitHub push is also working for `github.com/791806372-coder/onlythismuch`.
- The remaining App Store path is no longer "get certificates"; it is App Store Connect app setup, App Store provisioning profiles for the app/widget, clean App Store screenshots, Release archive/upload, and review metadata. The remaining public Mac path is Developer ID signing plus notarization/stapling before a GitHub Release asset.
- Continued source-side hardening while Apple signing remains external: `/v1/ping` now sanitizes the public Mac name at the HTTP boundary via `publicDeviceName`, so even a poisoned/manual connector config name such as `.local`, URL, IP, token-like, or email-like text cannot leak to the phone. `loadOrCreateConfig` now repairs unsafe stored public names back to `Mac connector`, while `loadConfig` remains read-only for status/doctor flows.
- Added connector regression coverage for that boundary: config tests prove unsafe stored names are repaired, server tests prove unsafe ping names return only `Mac connector`, and product invariants now include `Connector public name privacy`.
- Fixed an environment-dependent connector test assumption: status/doctor tests now assert advertised address count is positive and under `MAX_PAIRING_HOSTS`, rather than assuming this Mac always advertises exactly two addresses. This matters because the current Mac can advertise three local addresses.
- Current verification after this continuation: `npm run test:connector` passes 110 connector tests, `npm run check:product` reports 24 passes / 0 failures, `npm run check:scripts` passes, and full `npm run check` passes. `npm run device:preflight:local` now reports 16 passes / 4 warnings / 0 failures because a link-local fallback address is present; `npm run device:evidence:local` reports 34 passes / 16 warnings / 0 failures. `npm run device:next` still reports Apple Development signing as the next external gate.
- Fresh privacy hardening after subagent audit: shared Swift `PrivacySafeText` now rejects email, URL, localhost, `.local`, IPv4, token/secret/raw-output/provider-output-looking strings before snapshot decode, pairing save, or ping-name display. The app no longer renders raw saved Mac names or ping names; saved pairing UI uses only `已配 Mac · <address count> · 端口 <port>`, and unknown pairing-field errors no longer echo unsafe field names.
- The connector snapshot contract now rejects network-like string values before the phone sees them. Connector config now defaults the public device name to `Mac connector` and sanitizes unsafe existing config names, so `os.hostname()` / `.local` values do not flow into pairing, `/v1/ping`, or `/v1/snapshot`.
- Routine device/signing/install evidence output is tighter: `device:signing` and local preflight no longer print Team IDs, `device:next` requires private pairing config permissions before marking pairing ready, install summaries no longer retain device selectors, and install output no longer advertises raw devicectl JSON/log artifacts. Temporary connector config dirs created by smoke/preflight scripts are now removed in `finally` paths.
- Xcode project was regenerated after adding `ios/Shared/PrivacySafeText.swift`, and the native XCTest proof was rebuilt.
- Current verification after this continuation: `npm test` passes 27 tests, `npm run test:connector` passes 109 connector tests, `npm run check:ios`, `npm run check:product` reports 23 passes / 0 failures, `npm run check:scripts`, `npm run smoke:local-pairing`, and full `npm run check` all pass. `npm run device:preflight:local` reports 16 passes / 3 warnings / 0 failures. `npm run device:evidence:local` reports 34 passes / 16 warnings / 0 failures and writes `outputs/device-evidence/00-local-evidence-summary.json`.
- `npm run device:next` still reports the same external gate: sign into Xcode, create/download an Apple Development identity, then apply `DEVELOPMENT_TEAM`. This is not a source-code blocker; real signed install, iPhone WidgetKit screenshots, and strict post-install evidence still wait on Apple signing/device visibility.
- Local device-loop order stays `npm run device:preflight:local` before `npm run device:next`; strict real-device evidence still waits on Apple signing/device visibility.
- Aligned `npm run smoke:local-pairing` with the iPhone host-fallback model: it now passes when at least one advertised QR address serves ping plus a Swift-valid snapshot, while failed fallback attempts are summarized by count only.
- Cleaned stale README/status wording after subagent review: routine connector status output is now documented as config booleans plus advertised address counts, local pairing fallback failures are count-only, and LaunchAgent/doctor wording now reflects the current installed service state without raw host values.
- Current verification after the doc-consistency pass: stale-output text search is clean, `node scripts/check-device-evidence-docs.mjs` passes through `npm run check:scripts`, full `npm run check` passes, `npm run connector:status` prints only config booleans/port/address count/manual-pair commands, `npm run device:evidence:local` reports 35 passes / 15 warnings / 0 failures, strict `npm run device:evidence` still has the single expected failure `Signing team` because `DEVELOPMENT_TEAM` is empty, and `npm run device:next` points to Xcode Apple Development signing setup as the next gate.
- Widget taps now do real app-side work instead of only opening the app: `aiusage://refresh` reloads local state, records persistent `Proof 07` text, and runs the same LAN refresh/cache path as `测试连接` when a paired Mac exists.
- Added compact in-app QA proof captions for App Group, pairing/LAN/widget-tap, Widget LAN, final/offline widget, Mac-owned approval, and Local Network proof. First-LAN failures that look like permission/network denial now show a `Proof 10` Local Network settings hint without exposing hosts/tokens.
- The widget approval header now labels Mac-owned requests as `Mac待批🙃` and local UI fixtures as `本地测😏`, so approval screenshots cannot accidentally pass with a fixture-only request.
- Extracted widget-tap proof policy into shared executable Swift at `ios/Shared/WidgetTapProof.swift`: `WidgetRefreshDeepLink` owns `aiusage://refresh` recognition, `WidgetTapProofText` owns the persistent `Proof 07` app-open captions, and `LocalNetworkPermissionHint` owns the `Proof 10` first-LAN-failure settings hint. `ValidateFixtures` now executes this behavior directly and prints `validated widget tap proof helpers`.
- Updated the deeplink/product/evidence guards to follow that shared helper extraction instead of looking for the old inline `url.host == "refresh"` and `localNetworkPermissionHint(for:)` code. At that point, `npm run check` passed end to end and `npm run device:evidence:local` reported 34 passes / 13 warnings / 0 failures, with the remaining warnings limited to expected Apple signing / physical screenshot proof gaps.
- Hardened the iPhone proof runbook for the current Mac state where the optional LaunchAgent may already own port `8787`. `npm run device:runbook`, the root README, the iOS README, and `outputs/device-evidence/README.md` now tell testers to run `npm run connector:service:status` before starting mock, use the loaded service if it is running, or intentionally `npm run connector:service:unload` before `npm run start:mock`; they also warn to stop mock before `npm run start:real` or `npm run connector:service:load` because all three connector modes use port `8787`.
- `scripts/check-device-evidence-docs.mjs` now guards that service/mock/real connector ordering in the runbook and docs. Focused verification after this slice passed at that time with `npm run device:runbook`, `node scripts/check-device-evidence-docs.mjs`, `npm run check:scripts`, and `npm run device:evidence:local`; local evidence then reported 34 passes / 13 warnings / 0 failures.
- Tightened strict post-install evidence: before install proof exists, missing screenshots remain warnings so signing/install blockers stay clear; after `00-install-summary.json` proves a signed install, strict `npm run device:evidence` now adds a `Post-install screenshot proof` failure and exits nonzero until all required screenshots are present and sidecar-bound. The root README, iOS README, device-evidence README, and `scripts/check-device-evidence-docs.mjs` now document and guard this behavior.
- Current verification after the runbook/evidence-policy hardening: `node scripts/check-device-evidence-docs.mjs`, `npm run check:scripts`, `npm run device:runbook`, `npm run device:evidence:local`, and full `npm run check` pass. Strict `npm run device:evidence` still exits nonzero for the expected current blocker: empty `DEVELOPMENT_TEAM`; because install proof is not present yet, missing screenshot rows remain warnings for now.
- Extracted the screenshot proof blocking rule into `scripts/lib/device-evidence-policy.mjs` and added `scripts/check-device-evidence-policy.mjs`. The same policy now controls final post-install blocking plus per-screenshot and invalid-sidecar severity, so missing, corrupt, or unbound screenshot proof stays `WARN` before install proof / in local-only mode and becomes `FAIL` only after strict install evidence exists.
- Current verification after the executable screenshot-policy guard: `node scripts/check-device-evidence-policy.mjs`, `node scripts/check-device-evidence-docs.mjs`, `npm run check:scripts`, `npm run device:evidence:local`, and full `npm run check` pass. Strict `npm run device:evidence` still exits nonzero only for the expected `DEVELOPMENT_TEAM` signing blocker; screenshot rows are still warnings because `00-install-summary.json` is absent.
- Added two real-device approval diagnostic proof rows to `outputs/device-evidence/manifest.json`: `11-approval-app-diagnostic.png` for `App 上次` and `12-approval-widget-diagnostic.png` for `Widget 上次` in `快速批准`. The runbook, README, iOS README, and device-evidence README now require these paired-Mac diagnostics and reject using `本地 UI 测试请求` as acceptance proof.
- Added a read-only native XCTest build freshness guard: `npm run check:ios-tests:fresh` validates the built `AIUsageWidgetTests.xctest` and `AIUsageWidgetTests_iphoneos*.xctestrun` are present and newer than relevant iOS source/config inputs. `device:evidence` now includes this as `Native XCTest build proof`, and `check:scripts` runs the guard without rebuilding Xcode.
- Current verification after the approval-diagnostic evidence and XCTest freshness slices: `npm run check:ios-tests:fresh`, `node scripts/check-device-evidence-docs.mjs`, `npm run device:runbook`, `npm run check:scripts`, `npm run device:evidence:local`, and full `npm run check` pass. Local evidence now reports 35 passes / 15 warnings / 0 failures with 12 required screenshots. Strict `npm run device:evidence` still exits nonzero only for the expected `DEVELOPMENT_TEAM` signing blocker and reports 35 passes / 14 warnings / 1 failure.
- Tightened terminal approval guidance in `device:evidence` and `device:next`: when approval screenshots are missing, the printed loop now tells QA to capture app-origin `11-approval-app-diagnostic.png` with `App 上次` and widget-origin `12-approval-widget-diagnostic.png` with `Widget 上次`, not only the older 08/09 pending/decision screenshots. `scripts/check-device-evidence-docs.mjs` now guards those terminal strings.
- Current verification after the approval-guidance tightening: `node scripts/check-device-evidence-docs.mjs`, `npm run check:scripts`, `npm run device:evidence:local`, strict `npm run device:evidence`, and full `npm run check` ran. Full check passes. Strict evidence still fails only on empty `DEVELOPMENT_TEAM`, and the approval screenshot loop now prints both diagnostic capture steps.
- Hardened the same-Wi-Fi trust boundary: iOS now rejects `/v1/ping` or `/v1/snapshot` responses whose `deviceId` / `snapshot.device.id` does not match the saved pairing payload. The app and widget share this through `LocalHTTPSource`, and `npm run check:ios` now prints `rejected mismatched Mac identity`.
- Made Mac approval decisions retry-safe: if the Mac already recorded the same `requestId` + action and the phone retries after losing the first response, `POST /v1/approval-decisions` returns the stored bounded receipt instead of a false `invalid_approval_decision`, without appending a duplicate decision.
- Current verification after the LAN identity and approval-retry hardening: `npm test` passes 26 tests, `npm run test:connector` passes 107 connector tests, `npm run check:ios`, `npm run check:product` (22 passes / 0 failures), `npm run check:scripts`, `npm run check:ios-tests:fresh`, `npm run device:evidence:local`, and full `npm run check` pass. Strict `npm run device:evidence` still fails only on empty `DEVELOPMENT_TEAM`; `npm run device:next` still reports Apple Development signing setup as the next external gate.
- Added `npm run check:source-hygiene` so generated/vendor/local proof artifacts such as `work/XcodeGen`, `connector/dist`, `build`, `.swift-module-cache`, widget previews, generated evidence summaries, and `screenshot-proofs.json` are documented and ignored separately from authoritative source. `outputs/device-evidence/manifest.json` remains the source-like evidence spec.
- Current verification after the source-hygiene pass: `npm run check:source-hygiene`, `npm run check:scripts`, and full `npm run check` pass. Strict `npm run device:evidence` still reports 35 passes / 14 warnings / 1 failure, with the only failure `DEVELOPMENT_TEAM is empty`; `npm run device:next` still reports Xcode Apple Development signing setup as the next external gate.
- Tightened the pre-device proof surface after subagent review: screenshot manifest/README proof text now requires no raw LAN hosts for QR, app LAN, and widget LAN captures; the product guard now locks V1 to `.systemMedium` only; `LocalHTTPReport.shortSummary` now reports address indexes instead of raw hosts; `device:evidence`, `device:preflight`, `device:next`, and `device:signing` summarize local config/host/device proof with counts/placeholders instead of raw config paths, LAN hosts, or raw xctrace device lines.
- Hardened approval and secret handling: `/v1/approval-decisions` validates the current pending Mac request before reusing a stored retry receipt, so a newly seeded request with the same id cannot be cleared by a stale receipt; snapshot and approval safety scanners now reject `password`, `passphrase`, and `passwd` keys/values.
- Current verification after the subagent hardening pass: `npm test` passes 26 tests, `npm run test:connector` passes 108 connector tests, `npm run check:product` reports 23 passes / 0 failures, `npm run check:ios`, `npm run check:scripts`, `npm run device:preflight:local`, `npm run device:build:unsigned`, `npm run build:ios:tests`, `npm run device:evidence:local`, and full `npm run check` pass. Strict `npm run device:evidence` still reports 35 passes / 14 warnings / 1 failure, with the only failure `DEVELOPMENT_TEAM is empty`; `npm run device:next` still reports Xcode Apple Development signing setup as the next external gate.
- Hardened routine command output after subagent audit: `aiuw status`, `aiuw doctor`, `aiuw service status`, `device:next`, `device:evidence`, `device:install`, the AppIntent rebuild wrapper, and the native XCTest build wrapper now publish bounded counts, booleans, sanitized summaries, or relative log artifact paths instead of raw LAN hosts, connector config paths, local device names/selectors, Team IDs, absolute temp paths, or streamed Xcode output. Explicit QR/manual pairing remains the intentional pairing-address/payload surface.
- Current verification after the routine-output privacy pass: `npm run test:connector` passes 108 connector tests, `npm run check:scripts` passes, `npm run connector:status:live`, `npm run connector:doctor:live`, and `npm run connector:service:status` print sanitized address-count/service summaries, `npm run smoke:local-pairing` passes with address counts only, `npm run device:next` still reports Apple Development signing setup as the next external gate, `npm run device:evidence:local` reports 35 passes / 15 warnings / 0 failures, `npm run build:ios:tests` writes `build/ios-tests-build.log` and prints only completion status, `npm run check:appintents:fresh` passes, and full `npm run check` passes. Strict `npm run device:evidence` still exits nonzero only for the expected empty-`DEVELOPMENT_TEAM` signing blocker, reporting 35 passes / 14 warnings / 1 failure.
- Split the README device loop so strict `npm run device:evidence` is documented as the post-install screenshot gate, not part of the signing-blocked local readiness loop.
- Added `npm run smoke:service-program` for LaunchAgent command-path proof without installing launchd: it generates the real `aiuw service plist`, parses its `ProgramArguments`, starts that exact `node ... cli.js start --quiet-pairing` command with an isolated config dir and random port, then proves authenticated `/v1/ping`, `/v1/snapshot`, `aiuw status --live`, and `aiuw doctor --live` without leaking the pairing token.
- Fixed and installed the real Mac LaunchAgent proof path on this Mac. The generated plist now includes `WorkingDirectory`, `HOME`, `AIUW_CONFIG_DIR`, and a stable local/system `PATH` instead of inheriting transient shell paths. After reinstall/load, `npm run connector:service:status` reports `loaded=true`, `running=true`, `plistMode=0600`, and `program.detail="paths_ok"`.
- `npm run connector:doctor:live` and `npm run connector:status:live` now prove the installed LaunchAgent-created connector is ready for iPhone pairing with a private config, two advertised addresses reachable, and `snapshotLooksValid=true`, without printing the config path or raw host list.
- Hardened the subagent/Fable5 review gaps around the product contract: `account` metadata is no longer accepted in the TypeScript or Swift snapshot schema, CodexBar can only fill a connected/working/no-stable-source provider, approval request source/title now reject URLs/IPs/`.local`/`localhost`, persisted pairing JSON is raw-validated before Swift decoding, and `device:next` now honors `AIUW_CONFIG_DIR` for isolated connector config dirs.
- Current verification after this hardening slice: `npm test` passes 26 tests, `npm run test:connector` passes 105 connector tests, `npm run check:ios` passes with persisted-pairing and approval-network-detail coverage, `npm run check:product` reports 21 passes / 0 failures, `npm run check:scripts` passes, `npm run smoke:service-program` passes, `npm run build:ios:tests` succeeds with signing disabled, full `npm run check` passes, `npm run device:preflight:local` reports 16 passes / 3 warnings / 0 failures, `npm run device:evidence:local` reports 34 passes / 13 warnings / 0 failures at that time, and `npm run device:next` still reports the external gate as Xcode Apple Development signing setup.
- Tightened the iPhone pairing proof surface: the saved-pairing row now shows the Mac name, host count, and port instead of the first raw LAN IP or `.local` hostname. `npm run check:product` now includes this as a pairing-display privacy invariant, and the Swift validator/XCTest coverage prove the display summary does not include raw hosts or tokens.
- Current focused verification after this pairing-display privacy slice: `npm run check:product` reports 20 passes and 0 failures, `npm run check:ios` passes with `validated pairing display summary privacy`, and `npm run build:ios:tests` succeeds with signing disabled.
- Hardened the Fable5 review gaps around App Group and approvals: missing/unexpanded/invalid `AIUsageAppGroupIdentifier` now fails closed with visible M0 proof UI detail, app/widget Mac-owned approval clears only after a matching request-id/action receipt, and app-origin approval taps now write the same bounded local diagnostic trail as widget taps.
- Current focused verification after the App Group/approval slice: `npm run check:ios`, `npm run check:product` (19 passes / 0 failures at that point), `npm run check:ios-config` (25 passes / 1 expected signing warning / 0 failures), `npm run check:scripts`, `npm run smoke:local-approval`, `npm run build:ios:tests`, full `npm run check`, and `npm run device:next` all ran locally. `device:next` still reports the current external gate as Apple Development signing setup.
- Centralized physical-iPhone discovery into `scripts/lib/device-discovery.mjs`; `device:next`, `device:preflight`, `device:signing`, and `device:install` now share the same parser instead of each scanning raw output differently.
- Added fixture coverage for device-readiness false positives: failed `devicectl` JSON, unavailable/remembered/untrusted devices, simulator-only output, and nested valid physical-iPhone JSON are all checked by `scripts/check-device-discovery.mjs`.
- Re-ran `npm run check:product`, `npm run check:scripts`, and full `npm run check` successfully with the new parser check; keep `npm run device:preflight:local` first, then `npm run device:next` still reports the real current gate as Apple Development signing setup. `npm run device:signing` also runs cleanly and still reports the expected external blockers: empty `DEVELOPMENT_TEAM`, no Apple Development identity, CoreDevice service failure, and no xctrace-visible iPhone.
- Added WidgetKit approval-sync observability to `WidgetLANDiagnostic`: widget refresh now records whether the approval-request sync saved a Mac request, found no request, failed, or timed out before the usage snapshot fetch.
- The app's `Widget LAN 诊断` section now shows that approval-sync line while storing only bounded outcome/detail/status/count fields; raw hosts, URLs, tokens, request/response bodies, and raw logs remain excluded.
- Updated the Widget LAN screenshot proof text so `04-widget-lan.png` expects a success state (`Widget 摸到了`) plus approval-sync status, instead of allowing any skipped/failure diagnostic to look like success proof.
- Final verification after the Widget approval-sync diagnostic slice: `npm run check:ios`, `npm run check:product`, `npm run check:scripts`, `npm run build:ios:tests`, and full `npm run check` all pass locally.
- Tightened optional CodexBar adapter date validation to the snapshot contract's offset datetime shape. Parseable but offset-less reset times or `capturedAt` values now fail soft instead of reaching the snapshot contract and risking a connector crash.
- Added CodexBar regression cases for offset-less reset/capture timestamps; `npm run test:connector` and `npm run smoke:codexbar:fake` pass after the change.
- Hardened app-side LAN troubleshooting for device screenshots: the `测试连接` details now use a shared Swift formatter that renders address indexes (`地址 1`, `地址 2`) plus status/error captions instead of raw LAN IPs or `.local` hostnames, and redacts URL/token-like messages before display.
- Added Swift coverage and a product invariant for that display privacy path; `npm run check:product` now fails if app connection details drift back to direct `attempt.host` / raw `report.winningHost` rendering.
- Bounded WidgetKit-side refresh work: `WidgetRefreshCoordinator` now gives approval-request sync a short slice of a shared 3.5s widget refresh budget, then gives snapshot fetch the remaining budget. A slow approval fetch times out without starving `widget-lan` snapshot cache refresh.
- Added executable Swift validator coverage for the slow-approval path plus a product invariant that requires the coordinator budget, timeout wrapper, and regression case to stay present.
- Added a mocking-emoji product guard: `npm run check:product` now scans runtime/widget/fixture/preview-renderer sources for rejected gentle fallback emoji (`🤷`, `🍵`, `🥲`) and the old pointer emoji (`👉`), while allowing planning docs to keep those symbols as rejected design examples. Runtime header copy remains `别问了还剩这么点🤏`.
- Current focused verification after these local hardening changes: `npm run check:product` reports 19 passes and 0 failures, `npm run check:ios` passes including the executed slow-approval widget refresh regression, `npm run check:ios-config` reports 25 passes / 1 expected signing warning / 0 failures, `npm run check:scripts` passes, and `npm run build:ios:tests` succeeds with signing disabled.
- Added unsigned-build AppIntent metadata proof: `npm run check:appintents` reads both `Debug-iphoneos` widget metadata files (`AIUsageWidgetExtension.appex` and the app-embedded extension) and fails unless Xcode extracted exactly `ApprovalDecisionIntent`, `openAppWhenRun=false`, only `requestId`/`actionRawValue`, and no interactive/freeform text-like parameter.
- Added `npm run check:appintents:fresh` for aggregate checks. It first accepts already-fresh unsigned AppIntent metadata, rebuilds the unsigned iPhone app/widget only when metadata is missing/stale/invalid, then validates again. The freshness check now includes both `Metadata.appintents/extract.actionsdata` files directly, not just the surrounding app/widget bundles. Root `npm run check` uses this path before the Swift parser step so the check stays self-contained without provoking Xcode's transient destination lookup failure on every run.
- Integrated the same AppIntent metadata check into `npm run device:evidence:local` / strict evidence. The latest local evidence run now reports 31 passes, 16 warnings, and 0 failures, with four AppIntent metadata passes including direct `actionsdata` freshness.
- Tightened the WidgetKit approval-action product guard: `npm run check:product` now statically requires `ApprovalDecisionIntent.openAppWhenRun = false`, exactly two comment-stripped widget intent buttons in `ApprovalHeaderActions`, one `.approve` and one `.deny`, only `requestId`/`actionRawValue` AppIntent parameters, no message/comment/prompt/text input in the intent or app approval helper views, and balanced-body checks that widget `perform()` plus app `decideApproval` use `ApprovalDecisionHandler`/`.decide` without duplicating receipt logic.
- Added deterministic optional-M4 CodexBar subprocess proof: `npm run smoke:codexbar:fake` builds the connector, creates a temporary fake `codexbar` executable, runs it through the real `runLimitedCommand` subprocess path, feeds poisoned stdout/stderr plus unsafe extra JSON fields, and prints only sanitized Claude/Codex usage summaries. The real `npm run smoke:codexbar` remains manual because a live CodexBar install is optional.
- Current focused verification after this guard fix: `npm run check:appintents:fresh`, `npm run check:appintents`, `npm run check:product`, `npm run check:scripts`, `npm run device:evidence:local`, and full `npm run check` all pass.
- Hardened the real-device QA handoff: `npm run device:runbook` now tells testers to start the Mac connector before QR pairing, run `npm run connector:status:live`, and capture iOS Local Network permission proof separately from Mac-local reachability.
- Added `10-local-network-permission.png` to the device evidence manifest and README so denied Local Network permission cannot masquerade as generic same-Wi-Fi failure.
- Renamed the app's local approval fixture button to `本地 UI 测试请求`, and updated terminal/docs wording so `08/09` approval proof must start from the Mac-owned `npm run connector:approvals:seed` loop.
- Extended `scripts/check-device-evidence-docs.mjs` so the runbook, manifest, Local Network proof, and local-fixture rejection stay guarded.
- Current verification after this QA-handoff fix: `npm run check:ios`, `npm run check:product`, `npm run check:scripts`, `npm run device:build:unsigned`, `npm run device:evidence:local`, full `npm run check`, and `node scripts/check-device-evidence-docs.mjs` all pass.
- Current strict evidence remains blocked only by Apple signing: `npm run device:evidence` reports 35 passes, 14 warnings, and 1 failure: `Signing team`. The manifest now has 12 required screenshots.
- Fixed the final reviewer privacy gap: persisted Widget LAN diagnostic `detail` now redacts local/domain hostnames, not only URLs, IP addresses, and secret-like terms.
- Added explicit hostname-leak coverage in the native XCTest file, the Swift fixture validator, and the product invariant guard.
- Re-review result: both reviewer agents passed after the hostname redaction fix.
- Keep `npm run device:preflight:local` green before using `npm run device:next` to choose the next real-device gate.
- Current verification after this fix: `npm run check:product`, `npm run check:ios`, `npm run build:ios:tests`, and full `npm run check` all pass.
- Current next gate remains external Apple signing setup: `npm run device:next` reports empty `DEVELOPMENT_TEAM` and no usable Apple Development certificate.

## Implemented

- Root contract, strict Zod schema, raw safety scan, exact Claude+Codex V1 provider-set validation, six user-cacheable no-meter fixtures, and two internal meter-layout fixtures.
- Unavailable usage is now a minimal shape: `available=false` plus `reason`; source/sourceVersion/percent/reset keys are rejected even when they are `null`.
- Display-state coverage proving the user and internal fixtures together cover seven states without putting fake percentages in app-cacheable fixtures.
- Connector CLI package `aiuw` with:
  - `aiuw start --mock <fixture> --port 8787`
  - `aiuw start --port 8787` using safe Tier 0 local CLI collectors
  - `aiuw start --codexbar --port 8787` using explicit opt-in CodexBar usage parsing
  - `aiuw pair`
  - `aiuw status`
  - `aiuw status --live` plus root `npm run connector:status:live` for a redacted `/v1/ping` plus `/v1/snapshot` reachability/auth/snapshot-shape probe when the connector should already be running
  - `aiuw doctor` plus root `npm run connector:doctor` for a token-free Mac connector setup summary covering pairing config presence, LaunchAgent install/load/run state, installed LaunchAgent Node/CLI path health, and next actions
  - `aiuw doctor --live` plus root `npm run connector:doctor:live` for the same setup summary plus authenticated `/v1/ping` and `/v1/snapshot` proof when a pairing config exists
  - `aiuw approvals` plus root `npm run connector:approvals` for a read-only Mac-side approval summary with request id, action, timestamp, and pending request metadata only
  - `aiuw approvals seed|clear` plus root `npm run connector:approvals:seed` / `npm run connector:approvals:clear` for local Mac-loop testing of a bounded pending request without adding any prompt/message input
  - `aiuw service plist/install/load/unload/uninstall` plus root service scripts for a user LaunchAgent lifecycle
  - `aiuw service status` plus root `npm run connector:service:status` for a read-only plist/launchd state report, separate from authenticated connector reachability
  - root `npm run pair:manual` helper for explicit pasteable full-token pairing
  - bearer-protected `/v1/ping` and `/v1/snapshot`
  - separate bearer-protected `GET /v1/approval-request` for a Mac-owned pending request, with HTTP egress validation and `request:null` empty state
  - separate bearer-protected `POST /v1/approval-decisions` for approve/deny decisions, with no message/comment/prompt fields and no change to `/v1/snapshot`; the connector now rejects decisions that do not match the current pending Mac request and stores nothing for those attempts
  - private local pending-request JSON plus approval-decision JSONL storage under `.aiuw` with 0700 directory and 0600 file permissions
  - token rotation, including running-server reload after `aiuw pair`
  - QR payload rendering with redacted terminal JSON by default and explicit `--manual` output for pasteable full-token JSON; manual pairing rotates the local connector bearer token
  - read-only `aiuw status` pairing/config summary with config-present/configured booleans, port, advertised address count, and manual-pair command; default-port setup guidance prefers root npm scripts while preserving direct `aiuw` commands for non-default ports; missing config reports `pairing_config_missing` without creating `~/.aiuw/config.json`; routine status does not print raw LAN hosts, config paths, Mac device ids/names, tokens, or redacted pairing payloads
  - optional live status output with reachable/snapshot-ready address counts, `snapshotLooksValid`, and sanitized failure categories only, never tokens, authorization headers, raw host lists, raw exception text, snapshots, response bodies, or provider output
  - `--quiet-pairing` start mode for background service logs, so launchd startup does not log QR codes, pairing payloads, or bearer tokens
  - user LaunchAgent plist generation with absolute `ProgramArguments`, no shell strings, no `npm run`, private plist permissions, local logs, `RunAtLoad`, `KeepAlive`, and `ThrottleInterval`
  - explicit LaunchAgent load/unload commands using fixed `launchctl` arguments, with tests covering argv construction without calling launchctl
  - read-only LaunchAgent status inspection through fixed `launchctl print` argv, with sanitized summary output, installed Node/CLI path health, and no raw launchctl dump
  - periodically refreshed sanitized snapshot cache for real collector mode
  - LAN host filtering that keeps iOS-accepted local hosts, avoids duplicate `.local`, and rejects malformed Bonjour names
  - config dir/file permission handling
  - mock snapshot validation through the canonical contract
  - basic Codex/Claude CLI version/auth collectors with no usage percentages and no raw stdout/stderr in snapshots
  - Codex `doctor --json --summary` coarse health probe that reads only redacted `overallStatus`, never doctor detail fields, and can mark Codex degraded without faking usage
  - Tier 0 CLI discovery and subprocess PATH fallback through `PATH`, `~/.local/bin`, `~/.hermes/node/bin`, `/opt/homebrew/bin`, and `/usr/local/bin`, so connector launches outside an interactive terminal are less brittle
  - executable fallback discovery for Claude Code version-store binaries in `~/.local/share/claude/versions` and the bundled Codex CLI at `/Applications/Codex.app/Contents/Resources/codex`
  - Codex.app and Claude.app bundle discovery that reads only `CFBundleShortVersionString` via fixed-argv `plutil` as sanitized version fallback when CLI version output is unavailable; app presence does not change login, health, or usage semantics
  - conservative Claude third-party/no-meter classification for explicit CC Switch, OpenRouter, OpenAI-compatible, custom API base/provider, Vertex AI, Bedrock, or non-Anthropic base URL signals, while preserving official Anthropic base URLs as first-party unknown usage
  - opt-in CodexBar adapter that allowlists only exact normalized Claude/Codex provider names, sanitized version plus primary/secondary used percent and reset timestamps, converts used percent to remaining percent, drops raw identity/account/dashboard/non-version fields, ignores future/proxy provider names, and can only fill a connected, working Tier 0 provider whose usage reason is still `no_stable_source`; it cannot override logged-out, third-party/no-quota, degraded, error, or already-metered states
- Device preflight commands: `npm run device:preflight:local` checks local no-signing readiness by keeping App Group wiring, local-network/camera/privacy plist keys, generated Xcode project, connector build output, connector port, QR address candidate validation, optional LaunchAgent service status, token-safe temporary mock connector ping plus Swift-valid snapshot reachability through advertised QR addresses, while treating only empty `DEVELOPMENT_TEAM` and physical iPhone visibility as warnings; `npm run device:preflight` keeps those as hard real-device gates.
- Device next-step command with `npm run device:next`; it reads the current Mac state, detects Apple Development Team ID mismatch, parses only physical iPhones from `xctrace` stdout/stderr through the shared strict device-discovery helper, rejects failed/unavailable/remembered/untrusted/simulator `devicectl` JSON instead of substring-matching failed output, checks CoreDevice visibility before treating command-line install as ready, requires real connector `/v1/ping` plus `/v1/snapshot` evidence before treating pairing as ready, validates the live snapshot through the same built connector contract parser instead of a shallow shape check, prints the next real-device gate instead of making Kai infer it from several diagnostics, writes token-free pre-install readiness evidence to `outputs/device-evidence/00-device-readiness.json`, lists useful local preflight, unsigned compile, local evidence, and strict evidence commands while signing/device access is blocked, and uses the shared screenshot manifest plus centralized `screenshot-proofs.json` fingerprint checks when the flow reaches manual iPhone evidence capture. The readiness artifact now separates total missing screenshot proof from missing approval screenshot proof, records whether the root approval QA scripts are present, records `unsignedIphoneosBuildPresent` plus `unsignedIphoneosBuildFresh`, and keeps device visibility booleans false until signing/config gates are ready so transient Xcode/CoreDevice probes do not look like install readiness.
- The `device:next` screenshot gate now prints the Mac-owned approval QA loop when `08-approval-pending.png` or `09-approval-decision.png` is missing: seed with `npm run connector:approvals:seed`, tap `从 Mac 拉请求`, approve/deny from the app or widget, inspect with `npm run connector:approvals`, and clear with `npm run connector:approvals:clear`. `本地 UI 测试请求` remains explicitly UI-only proof before the paired-Mac loop.
- Device evidence commands with both a strict post-run audit and a signing-free local audit. Strict `npm run device:evidence` checks signing config, project wiring, App Group values in build products, source and bundled camera/local-network/privacy values, app/widget build products, signed installability of the iphoneos product, built AppIntent metadata for the approve/deny-only WidgetKit action, connector/service/live ping+snapshot evidence, and a machine-validated screenshot manifest with expected proof text, real PNG dimension checks, and centralized `screenshot-proofs.json` fingerprints. It validates live connector snapshots through the same built connector contract parser, rejects stale `Debug-iphoneos` app/widget products when iOS source/config changed after the last unsigned build, rejects broad connector config directory/file permissions, checks install summaries against the current app/widget product paths and App Group config, requires screenshot proof files to be newer than install evidence when an install summary exists, and writes token-free post-run audit evidence to `outputs/device-evidence/00-evidence-summary.json` plus a token-free screenshot fingerprint sidecar with bounded metadata only. Local `npm run device:evidence:local` uses the same source/build/AppIntent audit with signing downgraded to warning, writes `outputs/device-evidence/00-local-evidence-summary.json`, and intentionally does not replace strict evidence or bind screenshots. The screenshot README table is also checked against `manifest.json` so the human iPhone QA handoff cannot silently drift from the machine-verifiable source of truth.
- Device runbook command with `npm run device:runbook`; it prints a compact token-safe iPhone proof sequence from `outputs/device-evidence/manifest.json`, including the required screenshot filenames, Mac-owned approval QA loop, and two-pass `device:evidence` sidecar binding flow.
- Device screenshot evidence now requires the approval proof to start from a Mac-owned pending request: run `npm run connector:approvals:seed`, tap `从 Mac 拉请求`, then prove app/widget approve/deny clears only after the paired Mac receives the decision. The local `本地 UI 测试请求` path remains UI-only proof, not the main device acceptance path.
- No-signing iOS config drift guard with `npm run check:ios-config`; it verifies `Local.xcconfig`, `Local.xcconfig.example`, XcodeGen wiring, source plists/entitlements, QR scanner camera permission text, launch-screen config, iPhone target family, privacy manifests, and any existing simulator/iphoneos build products agree on `APP_GROUP_IDENTIFIER` while treating empty `DEVELOPMENT_TEAM` as a pre-signing warning.
- Built AppIntent metadata guard with `npm run check:appintents`; after `npm run device:build:unsigned`, it validates both direct and app-embedded `Debug-iphoneos` widget `Metadata.appintents/extract.actionsdata` files for exactly one `ApprovalDecisionIntent`, `openAppWhenRun=false`, exact `requestId`/`actionRawValue` parameters, no interactive inputs, and no text-like approval parameter.
- Generated-project drift guard inside `npm run check:ios-config`; every `ios/Shared/*.swift` file must appear in app, widget, and test target source phases, so adding a shared helper without rerunning XcodeGen is caught before Xcode build time.
- Connector package drift guard with `npm run check:connector-package`; it builds the connector, verifies `connector/package.json` and `connector/package-lock.json` agree on the `aiuw` bin path, and confirms the built CLI keeps its node shebang.
- Fresh-Mac setup entrypoint with `npm run setup:local`; it installs missing root and connector npm dependencies, creates missing `ios/Config/Local.xcconfig` from the example, generates the Xcode project when needed, builds the connector, runs the connector package guard, and runs the no-signing local device preflight without installing LaunchAgent, creating pairing config, rotating tokens, starting a connector server, changing Apple signing, or touching iPhone state. `npm run setup:local:check` is the read-only readiness check.
- Product-invariant drift guard with `npm run check:product`; it verifies runtime/source terminology stays on `usage` instead of allowance/额度 wording, the fixed widget header stays `别问了还剩这么点🤏`, runtime copy stays on the sharper mocking emoji direction instead of gentle fallback emoji or the old `👉` pointer, generated preview SVGs keep the same header/provider/no-fake-meter rules, contract/plan docs show minimal unavailable usage, fixtures remain exactly Claude + Codex, user-cacheable fixtures cannot grow fake percentages, Hermes/OpenClaw stay out of the V1 app/widget provider surface, offline widget states do not globally dim content below usable contrast, App Group fallback config fails closed and is visible in M0 proof UI, quick approval remains exactly two in-place widget actions (`approve` and `deny`) without message/comment/prompt/text input, app/widget approval taps route through the shared handler, Mac-owned approvals require a matching request-id/action receipt, app-side LAN troubleshooting and saved-pairing display stay raw-host-free for safer screenshots, persisted widget/approval diagnostics stay host/URL/body/token-free, widget LAN refresh stays centralized and budgeted in the shared coordinator, approval refresh cannot clear Mac-owned requests from `request:null`, and widget LAN/approval host attempts stay tied to the pairing schema cap instead of hardcoded smaller limits.
- Device build/install helper with `npm run device:build` and `npm run device:install`; it builds signed `Debug-iphoneos`, verifies the embedded widget extension, checks app/widget code signatures, embedded provisioning profile, and signed App Group entitlements, locates one available physical iPhone through the shared strict `devicectl` JSON / `xctrace` parser, installs with `xcrun devicectl device install app` only through the explicit install command, and writes token-free install evidence to `outputs/device-evidence/00-install-summary.json` after success. It does not start the connector, rotate pairing tokens, or change LaunchAgent state.
- Device signing diagnostic with `npm run device:signing`; it checks `Local.xcconfig`, Xcode first-launch state, Apple Development signing identities, Team ID identity match when inferable, provisioning profile folder presence, and physical iPhone visibility through the shared CoreDevice/xctrace parser without writing project state.
- Safe signing config helper with `npm run device:signing:apply`; after Xcode has created/downloaded an Apple Development identity, it updates only the `DEVELOPMENT_TEAM` line in `ios/Config/Local.xcconfig`, infers the Team ID only when exactly one local Apple Development identity exists, requires `--team TEAMID` when multiple teams exist, requires `--force` before writing a manual Team ID with no matching local identity or replacing a different existing Team ID, validates the 10-character uppercase Team ID shape, and does not print raw keychain output or device identifiers.
- Fixture-based widget SVG preview renderer with `npm run render:widget-previews`; it writes six safe user fixture previews, two internal meter-layout review aids, and seven device-proof header-state previews into `outputs/widget-previews` from the same root fixture contract, including pending approval and an old available-meter cache state that must render stale copy. The renderer self-validates generated SVGs for the fixed Chinese header, Claude/Codex labels, no allowance wording, no deferred Hermes/OpenClaw labels, and no percentages in safe no-meter previews.
- iOS scaffold with:
  - XcodeGen `project.yml`
  - generated `AIUsageWidget.xcodeproj`
  - app + WidgetKit extension targets
  - native `AIUsageWidgetTests` XCTest target generated from XcodeGen
  - configurable App Group entitlement through `APP_GROUP_IDENTIFIER`
  - `Local.xcconfig.example` with bundle prefix and App Group settings
  - app `NSCameraUsageDescription` for QR scanner permission, covered by config/evidence checks
  - app `UILaunchScreen` launch configuration plus iPhone-only app/widget target family, covered by config checks so unsigned iPhone builds avoid packaging warnings before signing work begins
  - app and widget `PrivacyInfo.xcprivacy` resources declaring App Group UserDefaults required-reason API use, no tracking, and no collected data
  - Swift snapshot model, pre-decode raw safety validation, strict known-field validation, root-contract scalar bounds for platform/id/name/version/source/error text, exact Claude+Codex provider-set validation, and rejection of unavailable/available usage contradictions, including unavailable-provider `capturedAt`
  - strict iOS pairing host validation aligned with connector local-host rules
  - persisted pairing rejects loopback hosts (`localhost` and `127.*`) because they point at the iPhone on a real device, not the Mac, and saved App Group payloads are revalidated on load/save before the app or widget can use them
  - pairing save fails loudly when App Group storage is unavailable or the saved payload cannot be read back
  - App Group proof scaffold with app-side write/read-back validation and visible widget header confirmation (`✓ Proof HH:mm:ss`)
  - visible proof-mode state in the app, so device QA can see when proof mode is blocking widget LAN refresh
  - manual pairing JSON entry with autocorrection/capitalization disabled and token-bearing text cleared after a successful save
  - manual pairing paste accepts a bounded terminal block containing `--manual` JSON, while redacted terminal/status output gets a specific manual-pairing error instead of a generic unknown-field message
  - in-app warning that redacted terminal JSON is not pasteable and `--manual` output is required for manual pairing
  - QR scanner wrapper
  - QR scanner sheet header with explicit `aiuw pair` guidance plus cancel/manual fallback actions; scanner delegate callbacks are delivered back to the main queue before mutating SwiftUI state
  - local HTTP ping/snapshot fetch
  - bounded app-side live LAN diagnostics for ping/snapshot attempts, address-index success, and HTTP status/error captions without raw LAN hosts, tokens, or response bodies
  - stale connection diagnostics are cleared when saving or failing to save a new pairing, so token-rotation evidence does not show old host attempts under a new paired Mac
  - immediate token redaction after pairing
  - pairing host-list cap shared by connector and iOS validation so widget LAN and approval AppIntent attempts stay bounded without dropping a valid fourth host
  - cache saved-at/source metadata for fixture and LAN snapshots, with the app Preview and widget sharing the same `LAN · 刚刚` / `Widget LAN · 刚刚` caption formatter for device proof
  - embedded Swift preview fixture JSON is kept strict and parity-checked against root fixtures, so PreviewProvider data does not rely on relaxed JSON-with-trailing-commas behavior
  - in-app preview from cached/fetched data, using the same proof-gated percent display and sharper unavailable copy as the widget
  - in-app controls to cache all six safe user fixtures and clear widget cache
  - user-cacheable preview fixtures keep every provider unavailable until a proven usage adapter exists, so fixture/demo states do not fake percentages
  - medium-only widget matching the dark two-column Claude/Codex direction
  - offline widget state keeps the explicit `Mac 摆烂了💤` status without globally dimming all content, preserving readability for the header and provider copy
  - available usage meters are marked stale when provider capture, snapshot update, or cache saved-at timestamps age past the freshness threshold, so old CodexBar/LAN values do not look live
  - Debug `PreviewProvider` gallery for normal, offline, and no-cache medium-widget states without relying on Xcode preview macros
  - no-cache widget fallback uses unknown/no-meter copy instead of normal fake percentages
  - widget-side best-effort LAN refresh from the saved pairing payload, using a short timeout and `widget-lan` cache source
  - widget-side App Group diagnostics for skipped/success/failed LAN refresh attempts so device QA can tell whether WidgetKit ran or the LAN fetch failed, without persisting hostnames, URLs, tokens, or response bodies
  - widget refresh approval-sync diagnostics attached to the Widget LAN diagnostic, so device QA can distinguish saved Mac-owned approval request, empty `request:null`, approval-sync failure, and approval-sync timeout without storing hosts, URLs, tokens, or bodies
  - shared widget refresh coordinator covering proof-mode skip, missing-pairing skip, successful `widget-lan` cache writes, pending approval save, `request:null` preservation of existing Mac-owned pending requests, approval refresh before usage snapshot fetch, and failure diagnostics without dropping the existing cache
  - distinct widget header labels for app-side `LAN` cache vs widget-originated `Widget LAN` cache
  - real no-cache widget header uses `没缓存😏` instead of the Xcode-only `预览 fixture` label, so first-run device proof is not mislabeled as a preview
  - Mac-owned quick approval flow with App Group pending-request cache, bounded fixture fallback for UI-only proof, approve/deny decisions, expiry handling, and no prompt/comment/message storage
  - shared approval decision handler for both the app buttons and widget AppIntent path, so local fixtures can clear locally while Mac-owned requests require a successful paired-Mac `POST /v1/approval-decisions` before they are recorded locally
  - local approval fixtures are labeled `source="本地测试"` with `local-fixture-*` ids and are the only requests that can clear through the local-only path; Mac-owned requests require a successful paired-Mac receipt from both the app and widget before they are recorded locally
  - in-app `快速批准` section with test request, approve, deny, and clear actions only
  - medium-widget approve/deny AppIntent buttons that keep the Claude/Codex usage layout intact
  - app-side `从 Mac 拉请求` action plus widget-side best-effort pending-request refresh through `GET /v1/approval-request`; successful `request:null` does not clear local Mac-owned pending state, and transport failure preserves it
  - app/widget approval submission to the paired Mac connector through `POST /v1/approval-decisions`; iOS clears a Mac-owned request only after the Mac returns `ok:true` with the same submitted request id and action, and if the paired Mac is unreachable, missing, or returns a mismatched receipt, the request stays pending
  - app button and widget AppIntent approval taps write one bounded local `ApprovalAttemptDiagnostic` into App Group for real-device QA; the app's `快速批准` section shows widget-originated records as `Widget 上次`, while the diagnostic stores only outcome, short detail, optional HTTP status, and attempted-host count, never tokens, URLs, hostnames, request/response bodies, prompt/comment text, raw logs, or provider output
  - strict App Group config-source visibility: missing, unexpanded, or invalid `AIUsageAppGroupIdentifier` values fail closed instead of silently accepting the hard-coded fallback; the app shows source detail in the M0 proof section so a free-account/capability issue is visible before QR/LAN testing
  - widget extension local-network plist metadata for same-Wi-Fi connector access
  - widget tap target through `aiusage://refresh`, with the app registering the URL scheme and showing a refresh prompt when opened from the widget
  - widget header observability for preview, fixture cache, LAN cache, proof mode, and offline snapshots
  - widget provider summaries and meter values expose explicit accessibility labels, with scaling on tight one-line labels
  - Fable 5 v2 copy rules: fixed `别问了还剩这么点🤏`, sharper no-meter/stale/offline copy, and deterministic fallback subline rotation
  - XCTest coverage for Swift-side `usage` contract decoding, exact Claude+Codex provider-set validation, rejection of legacy `allowance` fields, unavailable/available usage contradiction rejection including unavailable-provider `capturedAt` and present-null fallback/meter keys, unsafe raw field rejection, invalid platform/string-bound rejection, local-only pairing, bounded host lists, host-safe pairing display summaries, manual terminal-block pairing paste, redacted-payload rejection, Claude preview no-meter behavior, proof-mode storage, App Group identifier resolution, isolated pairing-store persistence plus invalid persisted-pairing rejection, usage freshness, approval store expiry handling, approval request GET behavior, minimal approval POST behavior including mismatched Mac receipt rejection, bounded approval-attempt diagnostics, shared approval decision handler behavior, and token-free widget/LAN diagnostics including sanitized transport errors
  - no-signing Swift validator coverage for embedded Swift preview fixture parity with root fixtures, invalid platform/string-bound rejection, oversized pairing paste/token rejection, host-safe pairing display summaries, manual terminal-block pairing paste, persisted pairing payload policy, redacted-payload rejection with the manual-pairing message, usage freshness stale-meter policy, App Group identifier resolution, widget refresh coordinator behavior, approval store validation, approval attempt diagnostic round-trip/safety, approval decision handler behavior including mismatched Mac receipt rejection, mocked LAN request behavior, bearer header on outbound requests, capped host attempts, minimal approval GET/POST bodies, 401 and transport errors sanitized into short diagnostics, and no token/header/hostname/response-body/raw URLSession text leakage into persisted App Group diagnostics

## Verified In This Workspace

- `npm test`
- `npm run test:connector`
- `npm run build:connector`
- `npm run check:connector-package`, currently confirming `connector/package.json` and `connector/package-lock.json` both point `aiuw` at `dist/connector/src/cli.js`, and that the built CLI keeps its `#!/usr/bin/env node` shebang.
- `npm run setup:local:check`, currently confirming Node.js 20+, root npm dependencies, connector npm dependencies, `Local.xcconfig`, generated Xcode project, connector build, connector package guard, and no-signing local device preflight are ready without installing LaunchAgent or creating pairing config.
- Connector unit coverage is now 108 tests, including approval decision parsing/storage/server routing, pending approval request parsing/storage/server routing, authenticated `GET /v1/approval-request`, response-boundary validation for unsafe pending requests, approval request source/title URL/host rejection, matching-decision pending-request clearing, rejection of decisions with no matching pending Mac request including injected-recorder bypass prevention, read-only approval summary output, approval seed/clear payloads, token-free connector doctor output, repo-friendly default-port npm-script setup guidance with direct `aiuw` fallback for non-default ports, stale LaunchAgent Node/CLI path detection, unreadable/invalid LaunchAgent plist degradation, collector subprocess PATH fallback, CLI candidate paths, Claude Code version-store executable discovery, Codex app-bundle executable discovery, app-bundle version fallback, Tier 0 providers omitting unavailable `capturedAt`, Codex doctor degraded-health mapping without raw output leakage, Claude third-party/no-meter classification without official-base-url false positives, exact CodexBar provider allowlisting that ignores aliases such as `Claude Code` and `OpenAI Codex`, CodexBar usage-only overlay behavior and version sanitization, CodexBar non-override coverage for logged-out, third-party/no-quota, and degraded Tier 0 states, sanitized `aiuw status --live` ping/snapshot transport failures, read-only missing-config `aiuw status`, connector config file and directory permission repair, LaunchAgent plist generation/install behavior, service JSON output without launchctl shell strings or local paths, launchctl argv construction without launchctl calls, read-only LaunchAgent status redaction, final HTTP snapshot egress validation against the exact Claude+Codex V1 shape, and stricter connector/iOS pairing host parity.
- `npm run connector:status:live` is implemented and covered by connector unit tests; it now reports reachable/snapshot-ready address counts plus `snapshotLooksValid` without printing raw host lists or snapshot bodies. Live command output still depends on a running connector in the user's active config.
- `npm run smoke:codexbar`, currently reporting `skipped: true` with `reason="codexbar_not_found"` because CodexBar is not installed/found in this shell; the command still validates the live probe path without printing raw CodexBar stdout/stderr.
- `npm run check:ios`, including exact Claude+Codex provider validation, strict embedded Swift preview fixture parity with root fixtures, root-contract invalid platform/string-bound rejection, unavailable/available usage contradiction rejection including unavailable-provider `capturedAt`, executed stdin pairing-validator wrapper behavior, executed oversized pairing paste/token rejection, executed manual terminal-block pairing paste, executed persisted-pairing payload validation, executed usage freshness stale-meter validation, executed App Group identifier resolution validation, executed redacted-payload rejection with the manual-pairing message, widget LAN diagnostic encode/decode, widget refresh coordinator behavior, local approval fixture labeling, rejection of local-only clearing for Mac-owned requests, approval request expiry rejection, approval attempt diagnostic round-trip/leak rejection, approval decision handler behavior including mismatched Mac receipt rejection, mocked LocalHTTP request behavior for snapshot plus approval GET/POST, and sanitized transport-error diagnostics that do not preserve raw URLSession text
- `npm run check:product`, currently reporting 21 passes and 0 failures for product terminology, V1 provider scope, no fake fixture meters, contract/plan unavailable-usage shape, widget header copy, mocking emoji tone, generated preview SVG invariants, real no-cache header copy, deferred-provider UI scope, offline contrast, approve/deny without message input, device proof captions, shared widget refresh coordinator use, approval `request:null` semantics, approval diagnostic privacy including app-origin records, Widget LAN plus approval-sync diagnostic privacy, app LAN display privacy, pairing display privacy, widget refresh budget, Mac-owned pending-request preservation, widget host-attempt cap consistency, App Group fallback fail-closed behavior, and approval receipt binding.
- `npm run check:ios-config`, currently passing with only the expected simulator-build App Group warning when no simulator product is present; it confirms the current source files, exact plist/entitlement/privacy/camera/launch-screen keys, iPhone target-family setting, generated shared Swift project references, XcodeGen target references, `SnapshotStore` runtime key with fallback fail-closed behavior, and existing iphoneos products all agree on `group.com.superzhangkai.aiusagewidget` for both app and widget.
- `npm run device:preflight:local`, now included in root `npm run check`, currently reporting 16 passes, 3 warnings, and 0 failures for no-signing local readiness on this network.
- `npm run build:ios:tests`, which compiles the generated native XCTest bundle for generic `iphoneos` with signing disabled through `scripts/build-ios-tests.mjs`. It is kept as a standalone Xcode proof rather than inside aggregate `npm run check`, because this Mac can transiently lose the generic iOS destination only inside long command chains after Swift/Xcode probes. The aggregate path still ensures unsigned AppIntent metadata is fresh, but skips the rebuild when the existing `Debug-iphoneos` app/widget metadata is already current.
- `npm run render:widget-previews`, which generated and self-validated 15 SVG previews in `outputs/widget-previews`: six safe user fixture previews, two internal meter-layout previews, and seven device-proof header-state previews for no-cache, proof mode, LAN cache, Widget LAN cache, old fixture cache, pending approval, and old meter cache.
- `npm run check:scripts`, covering device discovery false-positive fixtures, device preflight, device evidence, device runbook syntax, safe signing helper syntax/policy, unsigned iPhone build freshness policy, AppIntent metadata guard syntax, device-evidence README/manifest/readiness/evidence-summary artifact alignment, local-pairing proof, local setup script syntax, connector package-drift script syntax, and connector smoke script syntax.
- `scripts/check-device-next-policy.mjs` now also regression-checks that `device:next` keeps the Mac-owned approval QA commands in the screenshot gate instead of drifting back to fixture-only approval proof, and that CoreDevice readiness uses the shared strict device-discovery parser. `scripts/check-device-discovery.mjs` rejects failed `devicectl` JSON, unavailable/remembered/untrusted devices, and simulator-only output. `scripts/check-device-evidence-docs.mjs` also enforces the `08-approval-pending.png` and `09-approval-decision.png` manifest rows stay Mac-owned, mention the root approval scripts, reject local UI test requests as the acceptance path, keep the new approval-specific JSON fields in both readiness/evidence artifacts, and require `device:evidence`/`device:next` to share the centralized screenshot proof sidecar.
- Device evidence README/manifest alignment now machine-checks that approval screenshots reference `npm run connector:approvals:seed`, `从 Mac 拉请求`, and package scripts for seed/clear, preventing the manual proof path from drifting back to fixture-only approval.
- `npm run smoke:local-pairing`, also included in root `npm run check`, currently proves the mock connector manual pairing payload is accepted by the iOS Swift parser and that at least one advertised QR address can serve ping plus a Swift-decodable `/v1/snapshot`; failed fallback attempts are summarized by count without raw host values and do not fail the proof.
- `npm run smoke:local-approval`, also included in root `npm run check`, currently proves the mock connector Mac-owned approval loop without signing: it fetches a bounded pending request, submits a minimal approve decision, confirms the connector clears only after a matching request-id/action receipt, validates approval responses through the shared Swift decoder, and checks command outputs do not leak the bearer token.
- Direct Mac CLI approval loop verified through `npm run connector:approvals:seed`, `npm run connector:approvals`, and `npm run connector:approvals:clear`; it created only bounded pending request metadata under `.aiuw`, showed it in the read-only summary, and then cleared it so the final summary returned `pendingRequest:null`.
- `npm run device:signing` currently passes with warnings only under the personal Apple Developer team; local signing identities include Apple Development and Apple Distribution.
- `npm run device:install -- --allow-provisioning-updates` built and installed `AIUsageWidgetApp` on the connected iPhone with the personal App Group entitlement.
- `npx tsc -p tsconfig.json --noEmit`
- `npm run check`, now covering the local aggregate path: TypeScript tests, connector tests, connector package entrypoint drift guard, product guard, ensure-fresh built AppIntent metadata guard, Swift/static iOS checks, App Group/config guard, preview rendering, script checks, same-Wi-Fi local pairing smoke, Mac-owned approval smoke, generated LaunchAgent ProgramArguments smoke, deterministic fake CodexBar subprocess smoke, no-signing device preflight, and local-only evidence audit.
- `npm run smoke:connector`, including token-rotation behavior, default-output plaintext token leak checks, and `aiuw status --live` ping+snapshot readiness before/after token rotation
- `npm run smoke:service-program`, proving the generated LaunchAgent `ProgramArguments` boot the real connector command from an isolated config dir with `--quiet-pairing`, then pass authenticated ping/snapshot plus live status/doctor checks without writing a LaunchAgent plist.
- `npm run smoke:real-collectors`, proving this Mac currently emits a sanitized Tier 0 snapshot with Claude `2.1.173` connected/working and Codex `0.139.0` connected/degraded due to `status_probe_unavailable`, with both providers using `usage.reason="no_stable_source"` and no unavailable `capturedAt` or usage percentages.
- `npm run connector:service:plist`, confirming the generated LaunchAgent uses absolute Node/CLI paths, `--quiet-pairing`, local log paths, and no shell command.
- `npm run connector:service:status`, confirming the command emits only a sanitized LaunchAgent status summary. On this Mac it currently reports the `com.aiuw.connector` LaunchAgent is installed, loaded, running, private-mode `0600`, and has valid Node/CLI paths.
- `npm run connector:doctor`, confirming the current Mac reports a token-free setup summary for the existing pairing config and installed LaunchAgent; `npm run connector:doctor:live` additionally confirms `readyForIPhonePairing=true` when the running connector answers authenticated ping plus a contract-valid snapshot.
- `npm run device:next` should now be treated as a post-install evidence/screenshot guide rather than a certificate setup guide. Signing and command-line install have passed; the remaining real-device gates are App Group proof, same-Wi-Fi pairing, widget refresh/AppIntent proof, and screenshot evidence.
- Once the flow reaches screenshot capture, `npm run device:next` now gives the exact approval proof loop for the two approval screenshots so device QA proves a Mac-owned request rather than the app's local test request.
- `npm run device:evidence:local` remains useful for token-safe source/build/AppIntent/XCTest audit. Strict `npm run device:evidence` is now the screenshot/real-device proof gate after install evidence exists.
- Stripped-PATH real collector smoke using the connector build proved both local Claude and Codex can be detected through fallback paths: Claude `2.1.173` and Codex `0.139.0`, both `usage.available=false` with `reason="no_stable_source"`.
- iOS pairing payload parsing is now stricter and more forgiving where it helps at the QR/manual boundary: bounded `--manual` terminal blocks are accepted, redacted terminal/status payloads get a specific manual-pairing error, unknown fields are rejected before `Codable` can ignore them, Mac/device names are bounded, host counts and host lengths stay bounded for widget refresh, whitespace/oversized tokens are rejected before the payload is saved to App Group storage, and persisted App Group pairing payloads are revalidated on load/save so stale malformed data cannot bypass the QR/manual parser.
- QR scanner startup/support failures now surface as a visible manual-paste fallback instead of silently swallowing the scanner error. Invalid QR payloads stay visible in the scanner sheet instead of dismissing as if pairing had worked.
- Connector host ordering keeps `169.254.*` link-local addresses as last fallback, after better RFC1918 and Bonjour candidates, and JS host validation now rejects malformed IPv4 strings and malformed Bonjour names the same way the Swift parser does.
- The V1 snapshot contract is now intentionally narrower than the future roadmap: snapshots must contain exactly one Claude provider and one Codex provider, and exact percentages may use only `usage.source="codexbar"` until other adapters are implemented with privacy tests.
- `npm run device:build:unsigned` proves the arm64 iphoneos app and embedded widget compile with signing disabled before the Apple Team ID/device gate is resolved. The current unsigned build succeeds with app/widget iPhone target family and `UILaunchScreen` configured; the previous Xcode validation warnings about missing launch screen / broad orientation support are gone.
- The connector server revalidates snapshots at the `/v1/snapshot` response boundary and returns only `snapshot_validation_failed` if a future collector hands it unsafe or non-contract data.
- `npm run device:preflight:local` now reports 16 passes, 3 warnings, and 0 failures on this Mac/network: `DEVELOPMENT_TEAM`, connector port already in use, and physical iPhone visibility are warnings, while App Group/source wiring, Local Network/camera/privacy plists, Xcode project/tools, connector build, QR address candidate validation, Swift pairing parser, and mock QR snapshot reachability are hard local gates. `scripts/check-device-preflight-policy.mjs` regression-tests that missing local files/connector modules/mock QR readiness fail even in local-only mode.
- `npm run device:preflight` runs and currently reports expected failures until signing and device visibility are ready: empty `DEVELOPMENT_TEAM`, no Xcode-visible iPhone, or CoreDevice visibility problems. Its warnings summarize fallback address conditions and optional LaunchAgent state without raw host values. It checks camera/privacy/local-network source wiring, proves the full-token connector QR payload is accepted by the iOS Swift parser, and proves the temporary mock connector can serve ping plus Swift-valid `/v1/snapshot` from this Mac through advertised QR addresses.
- XcodeGen 2.42.0 generation through `npm run generate:ios`
- Xcode XCTest bundle build through standalone `npm run build:ios:tests`; execution of XCTest cases still depends on a working simulator or real-device runtime.
- Xcode simulator build remains optional in this workspace while CoreSimulator is unhealthy; the local proof path uses Swift static checks plus generic `iphoneos` compile/build proofs.
- Repeatable real Tier 0 collector smoke with sanitized output is now available as `npm run smoke:real-collectors`.
- CodexBar adapter unit tests for opt-in behavior, fixed argv/no shell runner shape, timeout/output caps, used-to-remaining conversion, invalid JSON/date/percent fail-soft behavior, dropping email/secret-like raw fields and non-version version text, and not overriding Tier 0 logged-out, third-party/no-quota, or degraded status.
- `npm run smoke:codexbar:fake`, now included in root `npm run check`, proves the optional CodexBar adapter through a deterministic fake CLI subprocess without relying on a live CodexBar install; it exercises the real limited-command runner, poisoned stdout/stderr capture, exact `codexbar usage --provider both --format json --json-only` argv, safe Claude/Codex provider allowlisting, used-to-remaining conversion, and token/email/transcript/raw-output-free summary printing.

The iOS check validates YAML, plists, entitlements, Swift parsing, Swift decoding of the six user fixtures plus internal layout fixtures, embedded Swift preview fixture parity with root fixtures, exact Claude+Codex provider-set enforcement, rejection of account metadata, root-contract scalar bounds such as platform and display-name length, no fake meters in user fixtures, unavailable/available usage contradiction rejection including unavailable-provider `capturedAt`, rejection of unsafe raw snapshot JSON, stdin pairing-validator wrapper behavior, oversized pairing host/text/token rejection, unknown/invalid pairing rejection, iOS pairing host rules, pairing display privacy, manual terminal-block pairing paste, persisted-pairing raw-field validation, usage freshness stale-meter policy, App Group identifier resolution, widget refresh coordinator behavior, host-free Widget LAN diagnostics, approval storage/submission, approval request source/title URL/host rejection, approval request GET shape, approval decision handler behavior for local fixtures and Mac-owned requests including matching receipt enforcement, approval attempt diagnostic safety, expired approval save rejection, redacted-payload rejection with the manual-pairing message, and mocked LAN request behavior. The iOS config check separately proves App Group source/build setting alignment, runtime fallback fail-closed behavior, and generated shared Swift source references without requiring Apple signing. The connector smoke tests prove the mock server can serve ping/snapshot, exposes `snapshotLooksValid` through `aiuw status --live`, accepts a newly rotated token while rejecting the old one, and that the same-Wi-Fi mock connector pairing payload reaches Swift-valid `/v1/snapshot` through advertised local addresses without printing raw host values. The standalone Xcode test build compiles the generated app, widget, and XCTest bundle with signing disabled for generic `iphoneos`. These are not substitutes for device testing.

## Still Not Verified

- Real iPhone 16 Pro Max App Group proof after the latest personal-team install.
- Same-Wi-Fi QR pairing and Local Network permission prompt after the latest install.
- Widget refresh behavior on device, including whether extension-originated LAN fetch works after the app triggers Local Network permission.
- Widget/AppIntent approve/deny behavior on the real iPhone, including paired-Mac success and offline pending behavior.
- The app/widget approval-attempt diagnostic on the real iPhone after a paired app button decision and a WidgetKit AppIntent tap.
- Native XCTest execution on a simulator/device. The test bundle compiles, but CoreSimulator is currently unavailable in this session.
- Final visual screenshot comparison against the reference widget on iPhone. Fixture SVG previews exist in `outputs/widget-previews`, but they are not real WidgetKit screenshots.
- Live CodexBar CLI usage verification on this Mac. `npm run smoke:codexbar` now exists as the safe opt-in probe, but `codexbar` is not currently found in this shell, so exact Codex/Claude usage percentages are implemented only through tested adapter parsing, not live local data.
- Full Tier 0 polish: stable Claude Desktop app-bundle CLI discovery if Anthropic exposes one, and broader Claude provider/third-party route detection if stable sanitized signals are found.

## Device-Test Environment Note

- The connected iPhone was visible enough for `npm run device:install -- --allow-provisioning-updates` to install the app on 2026-06-17.
- If device commands regress, unlock/replug/trust the iPhone and reopen Xcode Devices before debugging source code.

## Next Implementation Steps

1. Prove App Group sharing on the installed iPhone app/widget.
2. Re-pair the iPhone with the Mac connector if the bundle/App Group migration cleared old state.
3. Capture clean App Store screenshots for onboarding, paired dashboard, language/settings, medium widget, and small widget.
4. Create the App Store Connect app record for `com.superzhangkai.aiusagewidget`.
5. Confirm App Group and App Store provisioning profiles for the app and widget extension.
6. Archive `AIUsageWidgetApp` in Xcode and upload the Release build.
7. Developer ID sign the Mac connector, notarize, staple, zip, checksum, and then publish the GitHub Release asset.
8. Verify app-side LAN cache first, then clear cache and test widget-side `widget-lan` refresh from the saved pairing payload.
9. Use `npm run connector:approvals:seed`, tap `从 Mac 拉请求`, approve/deny from the app and widget, and confirm the request clears only when the paired Mac returns a matching request-id/action receipt; after app and widget taps, check `快速批准` for bounded diagnostics so failures are visible instead of swallowed. Use `npm run connector:approvals` to prove the Mac has only the bounded request id/action summary, then `npm run connector:approvals:clear` if you need to reset. Use `本地 UI 测试请求` only as local UI proof before the paired Mac loop.
10. Capture screenshots for App Group proof, LAN pairing, approval pending/action states, and widget rendering.
16. Save screenshots into `outputs/device-evidence` with the documented filenames, including `07-widget-tap-refresh.png`, run `npm run device:evidence` once to write `screenshot-proofs.json`, then run it again to validate the bound screenshot fingerprints.
17. Run the native XCTest scheme on a simulator or device once CoreSimulator/device runtime is healthy.
18. Continue M4 collectors after M0-M3 device gates are proven: install/verify CodexBar CLI live, look for any stable Claude Desktop app-bundle CLI path, and broaden Claude provider/third-party route detection only from sanitized signals.
